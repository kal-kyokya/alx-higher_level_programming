#!/usr/bin/env node
// Changes the text found inside an HTLM element on click

try {
  $(function () {
    $('#update_header').click( function () {
      $('header').text('New Header!!!');
    });
  });
} catch (e) {
  console.log(e);
}
